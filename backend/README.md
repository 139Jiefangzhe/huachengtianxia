# Backend (Optional)

This directory is reserved for custom microservices if needed.

Current architecture uses:
- Next.js frontend service
- Directus CMS service
- Postgres, MinIO, Redis

No custom backend service is required in stage 1.
