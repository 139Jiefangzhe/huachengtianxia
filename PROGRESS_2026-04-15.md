# HCTXF Stage-3 Progress (2026-04-15)

## 1) M0 Data Pipeline & Guardrails Implemented

- Extended schema bootstrap in `platform/scripts/bootstrap-directus-schema.mjs`:
  - New collections: `global_settings` (singleton), `category_layout_history`
  - New runtime fields on content collections: `pipeline_status`, `pipeline_attempts`, `pipeline_started_at`, `pipeline_finished_at`, `last_pipeline_error`, `quarantine_reason`
  - Added `categories.layout_config_version`
- Extended script client in `platform/scripts/lib/directus.ts`:
  - Added `updateItem`, `deleteItem`, `getItemById`, `listItems`
  - Upsert now returns previous snapshot (`existing`) for rollback operations

## 2) Migration Engine Upgraded

- Updated `platform/scripts/migrate-content.ts` with:
  - Batch processing (`--batch-size`) and rollback using saved write snapshots
  - Hard/soft gating policy:
    - Hard block -> `pipeline_status=archived`, `migration_status=needs_review`
    - Soft degradation -> preserve publishability (`migration_status=published`) with warnings
  - Placeholder replacement for low-severity missing images (`/static/missing-image.svg`)
  - Auto-archived report output (`--auto-archived-report`)
  - Stale importing recovery hook (`--stale-timeout-min`)
  - Added report metadata `canary_manifest_path`
- Added smoke validation command (passed):
  - `npm --prefix platform run migrate -- --mode dry-run ...`

## 3) New Stage-3 M0 Utility Scripts Added

- `platform/scripts/verify-state-consistency.ts`
- `platform/scripts/recover-stale-imports.ts`
- `platform/scripts/auto-heal-migration.ts`
- `platform/scripts/assert-canary.ts`
- `platform/scripts/generate-change-list.ts`
- `platform/scripts/crawl-static-snapshot.ts`
- `platform/scripts/switch-static-snapshot.sh`

## 4) Canary Assets Added

- `reports/canary/canary_manifest.json`
- `reports/canary/canary_synthetic.html`
- `reports/canary/canary_real_subset.tsv`

## 5) M1 Frontend Core Pages + Dynamic Layout Engine Implemented

- Rebuilt frontend structure with server data queries and fallback-safe rendering:
  - Routes: `/`, `/news/[slug]`, `/news/category/[slug]`, `/projects/[slug]`, `/transparency`, `/about/team`
  - Dynamic layout renderer with resilient block fallback
  - Layout error boundary + fallback panel
  - Rich text renderer with SSR quote fallback (`[quote:id=xxx]` replaced to readable blockquote)
  - Category layout mock hot reload support:
    - `ENABLE_LAYOUT_MOCK=true`
    - local files in `frontend/mocks/layouts/*.json`
- Added local assets/types/libs:
  - `frontend/public/static/missing-image.svg`
  - `frontend/types/*`, `frontend/lib/*`, `frontend/components/*`
- Updated `frontend/next.config.js`:
  - `output: 'standalone'`
  - expanded `images.remotePatterns`
  - exposes `ENABLE_LAYOUT_MOCK`

## 6) Validation Results

- Platform TypeScript check: PASS
  - `npm --prefix platform exec -- tsc --project platform/tsconfig.json`
- Frontend TypeScript check: PASS
  - `npm --prefix frontend exec -- tsc --project frontend/tsconfig.json`
- Frontend production build: PASS
  - `npm --prefix frontend run build`
  - Build succeeds with Directus 403 fallback behavior (non-blocking during empty/locked data phase)


## 7) Stage-4 SEO Fallback + Gray Switch Upgrades (This Run)

- Added `is_test_data` isolation end-to-end:
  - Schema bootstrap adds `is_test_data` on `articles/projects/categories/reports/quotes`.
  - Migration now parses canary manifest and writes `is_test_data` on canary article + quotes.
  - Frontend published queries now enforce `filter[is_test_data][_neq]=true`.
- Added frontend SEO fallback runtime endpoints:
  - `frontend/app/api/legacy-resolve/route.ts`
  - `frontend/app/sitemap.ts`
  - `frontend/app/robots.ts`
- Extended redirect generator for dual-track maps:
  - `platform/scripts/generate-redirects.ts` now supports:
    - `--include-test-data`
    - `--output-map-internal`
    - `--expected-total-internal`
  - Outputs:
    - public map (`legacy.map`, excludes canary/test by default)
    - internal map (`legacy.internal.map`, includes canary/test)
- Implemented Nginx gray switch architecture:
  - `platform/nginx/nginx.conf` adds map hash tuning + gray routing maps.
  - `platform/nginx/conf.d/hctxf.conf` uses `$legacy_redirect_target_effective`.
  - Added `platform/nginx/conf.d/redirects/gray-mode.conf` and `legacy.internal.map`.
  - Added ops script: `platform/scripts/set-gray-mode.sh`.
- Extended static fallback tooling:
  - `generate-change-list.ts`: added `--since`, excludes test data.
  - `crawl-static-snapshot.ts`: supports `--url-list` alias.
  - `switch-static-snapshot.sh`: flag-style args (`--version`, `--health-check`, etc.) + production confirmation guard.
  - Added `deploy-static-fallback.sh` and npm scripts.
- Added environment templates:
  - `.env.development`
  - `.env.production`

## 8) Command Execution Snapshot (This Run)

- Schema bootstrap:
  - `node platform/scripts/bootstrap-directus-schema.mjs --url http://localhost:28055 ...`
  - Result: `is_test_data` fields created.
- Import + consistency:
  - `npm --prefix platform run migrate -- --mode import ...`
  - `npm --prefix platform run verify-state-consistency -- ...`
  - Result: `inconsistencies=0`.
- Canary assert:
  - `npm --prefix platform run assert-canary -- ...`
  - Result: synthetic and real subset PASS.
- Redirect generation:
  - `npm --prefix platform run generate-redirects -- --expected-total 664 --expected-total-internal 665 ...`
  - Result: public=664, internal=665.
- Redirect validation:
  - `npm --prefix platform run validate-redirects -- --expected-total 664 --sample-size 10 ...`
  - Result: PASS, sample pass rate 100%.
- Frontend build:
  - `npm --prefix frontend run build`
  - Result: PASS, routes include `/api/legacy-resolve`, `/sitemap.xml`, `/robots.txt`.

## 9) Known Remaining Gate

- `npm --prefix platform run acceptance-stage2` currently fails at dry-run gate because migration dry-run error rate is `0.0887` (> `0.05`).
- This is data-quality gate behavior (script intentional), not an infrastructure failure.

## 10) Image Rendering Emergency Fix (2026-04-15 Late)

Root causes confirmed and fixed:
- Migrated HTML referenced `dry-*` pseudo file IDs (from dry-run cache reuse).
- Directus file upload failed with `NoSuchBucket` (`hctxf-assets` missing in MinIO).
- Public access to assets was blocked (`directus_files` public read permission missing).

Actions implemented:
- `platform/scripts/migrate-content.ts`
  - Import mode now purges `dry-*` hash cache entries before processing.
  - Dedup lookup now ignores `dry-*` IDs.
  - Added CLI option `--asset-timeout-ms` to reduce long remote wait on failed origins.
- `frontend/next.config.js`
  - Added `/directus/:path*` rewrite to Directus base URL so `localhost:3000` dev also resolves assets.
- `platform/scripts/bootstrap-directus-schema.mjs`
  - Added `ensurePublicReadPermission('directus_files')`.
- MinIO ops:
  - Created bucket `hctxf-assets` inside `hctxf-minio` via `mc`.

Data repair run:
- Re-import pass 1: first 50 pages after bucket fix.
- Re-import pass 2: resume remaining 615 pages.
- Reports:
  - `reports/migration/fix_images_import_50_report.json`
  - `reports/migration/fix_images_resume_full_report.json`

Validation snapshot:
- Directus files count: `459`
- Articles containing `/directus/assets/`: `375`
- Articles containing `/directus/assets/dry-`: `0`
- Cache files (`fingerprint_map.json`, `scripts/image-dedup-cache.json`) dry IDs: `0`
- Sample page `http://localhost:3000/news/nd004c` image assets return `200`.
